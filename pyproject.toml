[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "adventofcode"
version = "0.1.0"
description = "Advent of Code solutions"
requires-python = ">=3.8"

